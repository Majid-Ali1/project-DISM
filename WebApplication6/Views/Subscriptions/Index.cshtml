@model IEnumerable<WebApplication6.Models.Subscription>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/shared/_MyDashboard.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Monthly)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Yearly)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Monthly)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Yearly)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.SubscriptionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.SubscriptionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.SubscriptionId })
        </td>
    </tr>
}

</table>
